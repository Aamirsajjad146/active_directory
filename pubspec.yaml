name: msl_oauth_login
description: Azure Active Directory OAuth2 authentication package for Flutter with a ready-to-use login button.
version: 1.0.3
repository: https://github.com/Aamirsajjad146/active_directory

environment:
  sdk: ^3.12.2
  flutter: '>=3.12.2'

dependencies:
  flutter:
    sdk: flutter
  http: '>=1.1.0'
  webview_flutter: '>=4.14.0'
  flutter_secure_storage: '>=9.0.0'
  shared_preferences: '>=2.5.5'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: '>=5.0.0'
