name: msl_oauth_login_example
description: Example app demonstrating the msl_oauth_login package.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2
  flutter: '>=3.12.2'

dependencies:
  flutter:
    sdk: flutter
  msl_oauth_login:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
